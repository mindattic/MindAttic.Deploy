{
  "name": "mindattic-deploy",
  "private": true,
  "version": "1.0.0",
  "description": "Single-source generator and FTPS deployer for every MindAttic site (catalog landing pages, subfolder guides, root sites).",
  "scripts": {
    "build": "node src/build.js",
    "deploy": "node --use-system-ca src/deploy.js",
    "all": "node --use-system-ca src/deploy.js && node --use-system-ca src/deploy.js --sites && node --use-system-ca src/deploy.js --apps --include-disabled"
  },
  "dependencies": {
    "basic-ftp": "^5.0.5",
    "highlight.js": "^11.9.0",
    "marked": "^4.3.0"
  }
}
