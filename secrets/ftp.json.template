{
  "_comment": "Preferred: store these credentials via MindAttic.Vault at %APPDATA%\\MindAttic\\Ftp\\ftp.json (same shape) - DeployRunner picks that up automatically and this file becomes a fallback. Otherwise, copy this file to ftp.json (no .template suffix) and fill in real FTP credentials. The .gitignore excludes ftp.json. In CI, set MINDATTIC_FTP_JSON to the entire JSON object as a single GitHub Actions secret instead of committing a file.",

  "host":     "ftp.example.com",
  "port":     21,
  "user":     "user@example.com",
  "password": "REPLACE_ME",
  "secure":   true,

  "_rejectUnauthorized": "Optional. Defaults to true (server TLS certificate is validated). Only set to false for a host with a self-signed/expired cert you trust — it disables MITM protection."
}
