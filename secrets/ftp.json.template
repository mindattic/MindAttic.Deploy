{
  "_comment": "Copy this file to ftp.json (no .template suffix) and fill in real FTP credentials. The .gitignore excludes ftp.json. In CI, set MINDATTIC_FTP_JSON to the entire JSON object as a single GitHub Actions secret instead of committing a file.",

  "host":     "ftp.example.com",
  "port":     21,
  "user":     "user@example.com",
  "password": "REPLACE_ME",
  "secure":   true
}
